import * as fc from 'fast-check';
import { inspect } from 'util';
import { makeData } from './arbitraries';

test('samples', () => {
  // const result = fc.sample(makeData(), 5);
  // console.log(inspect(result, { depth: 5 }));
});
